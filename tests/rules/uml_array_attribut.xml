<checkerrules>
<!-- TICKETMANAGER -->
	<!-- ATTRIBUTE -->
	<!-- -tickets:Ticket[] -->
    <rule type="presence" points="4">
        <query>
            from t : V{TypeDeclaration}, f : V{FieldDeclaration}, m : V{Modifier}, at : V{ArrayType}, st : V{SimpleType}, vdf : V{VariableDeclarationFragment} with
                t.name="TicketManager" and
                t -->{TypeDeclarationBodyDeclarations1} f and
                f -->{FieldDeclarationModifiers} m and
                m.name="private" and
                f -->{FieldDeclarationType} at and
                at.dimensions=1 and
                at -->{ArrayTypeComponentType} st and
                st.name="Ticket" and
                f -->{FieldDeclarationFragments} vdf and
                vdf.name="tickets"
            report 0 end
        </query>
        <feedback prefix="UML Klassendiagramm">
            Das Attribut "private Ticket[] tickets" in der Klasse "TicketManager" fehlt.
        </feedback>
    </rule>
</checkerrules>
